var LaunchLine = function LaunchLine(x, y, style) {
  this.downX = x;
  this.downY = y;
  this.$svgNode = $('<svg height="100" width="100"></svg>');
  this.$lineNode = $('<line/>');
  this.$lineNode.setAttribute()
  
};

LaunchLine.prototype.redraw = function(x, y) {
  
};